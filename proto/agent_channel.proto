syntax = "proto3";
package agent_channel;

// bi-directional communication, initialed by agent
service AgentChannel {
  rpc Communicate(stream Message) returns (stream Message);
}

message Message {
  int32 message_type = 1;
  string message_data = 2;
}